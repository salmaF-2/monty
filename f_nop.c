#include "monty.h"
/**
 * f_nop - No operation. Doesn't do anything.
 * @stack: Pointer to pointer to the top of the stack.
 * @line_num: Line number
 */
void f_nop(stack_t **stack, unsigned int line_num)
{
(void) line_num;
(void) stack;
}
